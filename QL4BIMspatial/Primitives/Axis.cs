namespace QL4BIMspatial
{
    public enum Axis
    {
        X = 0,
        Y = 1,
        Z = 2
    }

    public enum AxisDirection
    {
        Positive = 0,
        Negative = 1
    }
}